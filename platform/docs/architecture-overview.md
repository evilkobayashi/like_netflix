# Architecture Overview
This platform implements a distributed enterprise operations architecture with an API gateway, 11 domain services, shared packages, async eventing via NATS, and distributed workers on BullMQ.
